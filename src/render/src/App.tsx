import React from 'react';
import LandscapeIcon from '@mui/icons-material/Landscape';
import FileOpenIcon from '@mui/icons-material/FileOpen';
import { AppBar, Button, Box, Container, Typography, Toolbar, IconButton, CssBaseline } from '@mui/material';

export function App() {
  async function initTorrentDownload() {
    //TODO
  }
  return (
    <>
      <Box sx={{
        flexGrow: 1,
        whiteSpace: 'nowrap',
        button: {
          color: 'inherit',
        },
      }}>
      </Box>
    </>
  )
}